function changeFirstAndLast(str) {
    if (str.length <= 1) {
        return str;
    }
    return str.charAt(str.length - 1) + str.substring(1, str.length - 1) + str.charAt(0);
}

// Example usage
console.log(changeFirstAndLast("hello")); // Output: oellh
